
(function(){
  function placeGPS(){ /* disabled */ return; }

  if(document.readyState === 'loading'){
    /* placeGPS listener disabled */
  } else {
    /* placeGPS() call disabled */
  }
})();
